# Backend requirements for Property Management System

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
pydantic-settings==2.5.2
email-validator==2.2.0

# Database — SQLAlchemy ORM + PyMySQL driver + Alembic migrations
sqlalchemy==2.0.43
alembic==1.16.5
pymysql==1.1.3
cryptography==42.0.8

# Environment variables
python-dotenv==1.0.0

# Security
PyJWT==2.13.0

# Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Data handling
python-multipart==0.0.9

# Generación de PDFs de factura (plantilla HTML del recibo)
jinja2==3.1.6
# NOTA: la conversión HTML→PDF usa Chromium/Chrome headless (subprocess --print-to-pdf),
# que NO es un paquete pip sino un BINARIO DE SISTEMA. Debe instalarse en el entorno de
# ejecución (en la imagen Docker de producción: `apt-get install -y chromium`). La ruta del
# binario se puede fijar con la variable de entorno BILL_PDF_CHROMIUM_PATH; si se deja vacía,
# el renderer autodetecta chromium/chromium-browser/google-chrome/chrome del PATH.

# Testing
pytest==8.4.2
pytest-mock==3.15.1
pytest-cov==7.1.0
httpx==0.28.1
paramiko==3.5.1
scp==0.15.0
