[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: tests unitarios sin I/O ni dependencias externas
    integration: tests que combinan dos o más módulos reales
    e2e: tests de flujo completo de extremo a extremo
